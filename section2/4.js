// -----------------------------------
// 4. Dynamic Typing
// -----------------------------------
// the type of variable can change at runtime
let item = "Banana";
console.log("item", item, "is a", typeof item);
item = 100;
console.log("item", item, "is now a", typeof item);
