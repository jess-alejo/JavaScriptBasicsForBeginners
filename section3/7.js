// -----------------------------------
console.log("7- Logical Operators");
// -----------------------------------

// Logical AND (&&)
// Returns TRUE if both operands are TRUE
console.log(true && true);

// Logical OR (||)
// Returns TRUE if one of the operands is TRUE
console.log(true || false);

// NOT (!)
// Opposite of the logical operator result
console.log(!true);
